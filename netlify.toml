[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

# Route any /tcg/* request to the function (no CORS in browser)
[[redirects]]
  from = "/tcg/*"
  to   = "/.netlify/functions/tcg-proxy"
  status = 200
